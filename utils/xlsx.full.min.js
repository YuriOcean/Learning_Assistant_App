export function validateSchedule(data) {
  const requiredFields = ['courseName', 'weekday', 'startTime']
  return data.every(item => 
    requiredFields.every(field => 
      item[field] && item[field].trim().length > 0
    )
  )
}